# Dataloop project ID (required — find it in your project settings)
DTLPY_PROJECT_ID=your_project_id_here

# Dataloop environment (usually "prod")
DTLPY_ENV=prod

# Your Dataloop authentication token
# To get your token:
#   1. Log into https://console.dataloop.ai
#   2. Open browser DevTools (F12) > Console
#   3. Run: await dl.token()
#   Or via Python:
#   import dtlpy as dl; dl.login(); print(dl.token())
DTLPY_TOKEN=your_token_here
